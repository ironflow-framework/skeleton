<!DOCTYPE html>
<html lang="{{ APP_LOCALE }}" data-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% block title %}
                IronFlow
            {% endblock %}
        </title>
        <link rel="icon" href="{{ asset('images/ironflow.ico') }}" type="image/x-icon" />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet" />
        {{ vite_assets('app.js') }}
    </head>
    <body class="bg-white dark:bg-gray-900 transition-colors duration-200">
        <nav class="bg-white dark:bg-[linear-gradient(135deg,#1a1a1a_0%,#2a2a2a_100%)] shadow-lg">
            <div class="max-w-7xl mx-auto px-4">
                <div class="flex justify-between h-16">
                    <div class="flex">
                        <div class="flex-shrink-0 flex items-center">
                            <a href="/" class="text-xl font-bold text-gray-800 dark:text-white">IronFlow</a>
                        </div>
                        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                            <a href="/products" class="border-transparent text-gray-500 dark:text-gray-300 hover:border-gray-300 hover:text-gray-700 dark:hover:text-white inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                                Produits
                            </a>
                            <a href="/categories" class="border-transparent text-gray-500 dark:text-gray-300 hover:border-gray-300 hover:text-gray-700 dark:hover:text-white inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                                Catégories
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </nav>

        {# Contenu principal #}
        <main>
            {# Contenu de la page #}
            {% block content %}{% endblock %}
        </main>
        
        {% block javascript %}{% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
        <script src="/js/notifications.js"></script>
    </body>
</html>
